// omp_atomic.cpp
// compile with: /openmp
#include <stdio.h>
#include <omp.h>

#define MAX 10

int main() {
    int numthread = omp_get_num_threads();
    for (i = 0; i < n; i+1)
    if (dados[i]=duo)
#pragma omp critical
            cont++;
print("cont=%0")                
}
